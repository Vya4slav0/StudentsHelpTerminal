<Page x:Class="StudentsHelpTerminal.Views.SettingsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:StudentsHelpTerminal.Views"
      xmlns:vm="clr-namespace:StudentsHelpTerminal.ViewModels"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="SettingsPage">

    <Page.DataContext>
        <vm:SettingsPageViewModel/>
    </Page.DataContext>

    <Grid Background="White">

        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>

        <DataGrid Grid.Row="0" Margin="10" ItemsSource="{Binding SettingsList}">
            
        </DataGrid>

        <WrapPanel Grid.Row="1" Orientation="Horizontal" Margin="10">
            <Button Content="Save"/>
            <Button Content="Back" Command="{StaticResource NavigateBackCommand}"/>
        </WrapPanel>
    </Grid>
</Page>
